# AI Provider Configuration
ANTHROPIC_API_KEY=sk-ant-xxxxx
CLAUDE_MODEL=claude-sonnet-4-5-20250929
AI_TIMEOUT_SECONDS=120
AI_MAX_TOKENS=8000

# Telegram Notifications
TELEGRAM_BOT_TOKEN=1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHANNEL_ARCHIVE_ID=-1001234567890
TELEGRAM_CHANNEL_ALERTS_ID=-1009876543210

# Log Source Configuration
# Options: "logwatch" (default) or "drupal_watchdog"
LOG_SOURCE_TYPE=logwatch

# Logwatch Configuration (used when LOG_SOURCE_TYPE=logwatch)
LOGWATCH_OUTPUT_PATH=/tmp/logwatch-output.txt

# Drupal Watchdog Configuration (used when LOG_SOURCE_TYPE=drupal_watchdog)
# Path to Drupal project root (for export script)
DRUPAL_ROOT=/var/www/html
# Path to the watchdog export file (JSON or drush output)
DRUPAL_WATCHDOG_PATH=/tmp/drupal-watchdog.json
# Format of the watchdog file: "json" (recommended) or "drush"
DRUPAL_WATCHDOG_FORMAT=json
# Optional: Site name for multi-site deployments
DRUPAL_SITE_NAME=
# Minimum severity level to include (0-7, default: 3=error)
# 0=emergency, 1=alert, 2=critical, 3=error, 4=warning, 5=notice, 6=info, 7=debug
# Setting to 3 means: include errors and higher (emergency, alert, critical, error)
DRUPAL_MIN_SEVERITY=3
# Maximum number of entries in output file (default: 100)
DRUPAL_WATCHDOG_LIMIT=100

# Common Log Settings
MAX_LOG_SIZE_MB=10

# Application Settings
LOG_LEVEL=info
ENABLE_DATABASE=true
DATABASE_PATH=./data/summaries.db

# Preprocessing (for large log files)
ENABLE_PREPROCESSING=true
MAX_PREPROCESSING_TOKENS=150000

# Network Proxy (optional)
HTTP_PROXY=
HTTPS_PROXY=
