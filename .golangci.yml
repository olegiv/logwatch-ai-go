version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - errorlint
    - gocritic
    - modernize
    - unconvert
    - unparam
    - usetesting
    - misspell
    - revive
    - dupl
    - gocyclo
    - gosec

  settings:
    staticcheck:
      checks:
        - all

    govet:
      enable:
        - unusedwrite

    gocritic:
      disabled-checks:
        - ifElseChain

    revive:
      rules:
        - name: var-naming
        - name: indent-error-flow
        - name: errorf

    gocyclo:
      min-complexity: 20

    dupl:
      threshold: 150

  exclusions:
    generated: lax
    rules:
      - linters: [unparam, dupl]
        path: _test\.go
      # Tests legitimately read fixture paths, write temp files with relaxed
      # permissions, and use predictable test data — not a production surface.
      - linters: [gosec]
        path: _test\.go
      - linters: [gocyclo]
        path: cmd/analyzer/main\.go

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
