# Copy this file to deploy/deploy.env and edit. Sourced by deploy/ssh helper
# scripts; gitignored so it stays personal.

# Required: SSH target. Either a bare hostname (defaults to root@<host>)
# or an explicit user@host.
DEPLOY_HOST=server.example.com
